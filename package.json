{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwind build -i src/style.css -o public/styles.css",
    "watch": "tailwindcss -i src/style.css -o public/styles.css --watch",
    "dev": "concurrently \"npm run watch\" \"live-server public --watch=public\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.0"
  }
}
